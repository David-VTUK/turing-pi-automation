kanboard:
  application:
    port: 80
    additionalLabels: {}
    persistence:
      enabled: true
      persistentVolumeClaim:
        name: kanboard-pvc
        create: true
        storageClass: longhorn
        size: 5Gi
        accessMode: ReadWriteOnce
  route:
    enabled: true
    hostnames:
      - kanboard.virtualthoughts.co.uk
    parentRefs:
      - name: cilium-api-gateway
        namespace: cilium
        sectionName: https-gateway
