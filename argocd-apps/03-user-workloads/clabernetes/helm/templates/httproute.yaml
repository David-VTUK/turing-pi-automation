apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: clabernetes-route
  namespace: clabernetes
spec:
  hostnames:
    - "clabernetes.virtualthoughts.co.uk"
  parentRefs:
    - group: gateway.networking.k8s.io	
      kind: Gateway
      name: cilium-api-gateway
      namespace: cilium
      sectionName: https-gateway
  rules:
    - backendRefs:
        - group: ''	
          kind: Service
          name: "clabernetes"
          port: 80
          weight: 1	
      matches:	
        - path:	
            type: PathPrefix	
            value: /         